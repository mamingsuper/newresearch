import test from'node:test';import assert from'node:assert/strict';import{readFile}from'node:fs/promises';import{handleConfirmProgramRequest}from'../supabase/functions/confirm-program-import/index.ts';
const ID='11111111-1111-4111-8111-111111111111';
test('confirmation calls atomic RPC only after admin verification',async()=>{const req=new Request('https://x',{method:'POST',headers:{authorization:'Bearer x','content-type':'application/json'},body:JSON.stringify({submissionId:ID})});let calls=0;const result=await handleConfirmProgramRequest(req,{authenticate:async()=>({id:ID,role:'admin'}),confirm:async()=>{calls+=1;return{status:'imported'};}});assert.equal(result.status,200);assert.equal(calls,1);const source=await readFile(new URL('../supabase/functions/confirm-program-import/index.ts',import.meta.url),'utf8');assert.match(source,/confirm_program_import/);assert.doesNotMatch(source,/from\(['"]papers/);});
