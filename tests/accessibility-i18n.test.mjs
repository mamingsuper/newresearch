import test from'node:test';import assert from'node:assert/strict';import{readFile}from'node:fs/promises';import{dictionariesForTest}from'../public/i18n.js';
test('English and Chinese dictionaries cover identical product states',()=>{const{en,zh}=dictionariesForTest();assert.deepEqual(Object.keys(zh).sort(),Object.keys(en).sort());});
test('production UI preserves focus and reduced-motion behavior',async()=>{const css=`${await readFile(new URL('../public/styles.css',import.meta.url),'utf8')}\n${await readFile(new URL('../public/results-v2.css',import.meta.url),'utf8')}`;assert.match(css,/:focus-visible/);assert.match(css,/prefers-reduced-motion:\s*reduce/);const html=await readFile(new URL('../public/index.html',import.meta.url),'utf8');assert.match(html,/role="status"/);assert.match(html,/aria-live="polite"/);});
