import test from 'node:test';import assert from'node:assert/strict';import{readFile}from'node:fs/promises';
test('workflows use Node 24-compatible actions and enforce page budgets',async()=>{for(const name of['ci.yml','pages.yml']){const value=await readFile(new URL(`../.github/workflows/${name}`,import.meta.url),'utf8');assert.match(value,/actions\/checkout@v6/);assert.match(value,/actions\/setup-node@v6/);assert.match(value,/pages:budget/);assert.doesNotMatch(value,/checkout@v4|setup-node@v4|configure-pages@v5/);}const pages=await readFile(new URL('../.github/workflows/pages.yml',import.meta.url),'utf8');assert.match(pages,/configure-pages@v6/);});
