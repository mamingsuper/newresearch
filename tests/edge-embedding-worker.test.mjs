import test from'node:test';import assert from'node:assert/strict';import{processBatch}from'../supabase/functions/_shared/edge-embedding-worker.ts';
const job={paper_id:'1',input_hash:'a',model:'text-embedding-3-small',dimensions:512,attempts:1,title:'T',abstract:'A',conference_name:'ICA',conference_year:2027};
test('worker accepts only 512-dimensional production embeddings',async()=>{let complete=0;const ok=await processBatch({jobs:[job],embed:async()=>[Array(512).fill(.01)],complete:async()=>{complete+=1;return true;},release:async()=>true});assert.equal(ok.completed,1);assert.equal(complete,1);let released='';const bad=await processBatch({jobs:[job],embed:async()=>[[.1]],complete:async()=>true,release:async(_j,code)=>{released=code;return true;}});assert.equal(bad.failed,1);assert.equal(released,'invalid_embedding_dimensions');});
